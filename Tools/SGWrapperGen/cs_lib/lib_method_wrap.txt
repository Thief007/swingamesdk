public static %(return_type)s(%(params)s)
{
    try
    {
        %(returns)s DLL_%(name)s(%(args)s);
    }
    catch (Exception exc)
    {
        throw new SwinGameException(exc.Message);
    }
    #if DEBUG
    finally
    {
      if (DLL_sg_Core_ExceptionOccured() != 0)
      {
          StringBuilder sb = new StringBuilder(2048);
          DLL_sg_Core_ExceptionMessage(sb);
          throw new SwinGameException(sb.ToString());
      }
    }
    #endif
}